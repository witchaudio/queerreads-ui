// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'books.google.com',
        'media.giphy.com',
        'lh3.googleusercontent.com',
      ],

    },
  };
  
  export default nextConfig;
  